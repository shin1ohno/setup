# coralline config — managed by cookbooks/claude-code (setup repo).
# coralline pinned to commit 04808390 (2026-06-14). Local-only: no network/API.
# Deployed to ~/.claude/coralline.conf and sourced by ~/.claude/coralline/statusline.sh.

# Theme palette. Swap this single source line to change theme
# (available: claude-coral catppuccin-mocha nord gruvbox-dark tokyo-night mono;
#  vendor the matching themes/<name>.conf alongside it).
. "$HOME/.claude/coralline/themes/claude-coral.conf"

VL_STYLE="pill"        # pill: powerline capsules · lean: flat colored text
VL_LAYOUT="auto"       # wrap into VL_MAX_LINES rows when the window narrows
VL_MAX_LINES=2
VL_CLOCK="24h"         # JST operation; coralline default is 12h
VL_NAME_MAX=16         # truncate long branch/project names (e.g. docs/refactoring-plan-caseB)
# Segments left at coralline defaults: dir git model ctx limit5h limit7d cost clock
