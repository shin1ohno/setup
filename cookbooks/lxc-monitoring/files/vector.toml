# Vector pipeline for RTX router syslog → Elasticsearch ingestion.
#
# Why Vector and not Promtail's syslog target:
#   RTX1210 Rev.14 / RTX830 Rev.15 firmware emit non-standard syslog —
#   `<PRI>tag msg` with no TIMESTAMP and no HOSTNAME. Both RFC5424 and
#   RFC3164 strict parsers (used by Promtail / Grafana Alloy via the same
#   leodido/go-syslog library) reject these packets at the timestamp
#   column, so Promtail records `parsing_errors_total` per packet and
#   forwards nothing. Vector's `socket` source ingests the raw UDP payload
#   and lets a VRL `remap` transform decode the format manually, with
#   wall-clock at receive time used for the event timestamp (LAN-local,
#   sub-ms latency relative to RTX-side event time).
#
# Phase 6 (ADR 0005): Loki sink + container removed. Elasticsearch is the
# sole sink for parsed RTX events; Kibana provides the analyst UI. The
# `parse` transform's structured fields (src/dst/severity/router/...)
# carry over unchanged into the ES document mapping.

# -----------------------------------------------------------------------------
# Global config
# -----------------------------------------------------------------------------

# Disk buffer root. Both [sinks.loki] and [sinks.elasticsearch] declare
# buffer.type = "disk" — Vector requires data_dir for disk-buffer storage.
# Bind-mounted from /data/monitoring/vector on the host (see
# cookbooks/lxc-monitoring/files/docker-compose.yml). Per-sink buffer
# subdirectories under data_dir/buffer/v2/<sink_id>/ are created by Vector
# at startup; the cookbook ensures the parent dir exists.
data_dir = "/var/lib/vector"

# -----------------------------------------------------------------------------
# Enrichment tables
# -----------------------------------------------------------------------------

# GeoIP via MaxMind/db-ip mmdb. Loaded once, queried per event from the
# `parse` VRL transform via get_enrichment_table_record.
# Vector 0.42+ removed the standalone `transforms.geoip`; lookups now go
# through the enrichment_tables API.
[enrichment_tables.dbip_city]
type = "mmdb"
path = "/etc/vector/geoip/dbip-city-lite.mmdb"

# -----------------------------------------------------------------------------
# Sources
# -----------------------------------------------------------------------------

[sources.rtx_udp]
type = "socket"
mode = "udp"
address = "0.0.0.0:514"
# .timestamp is auto-set to receive-time. .host is the sender IP. .message
# carries the raw UDP payload (RTX `<PRI>tag msg` text).
max_length = 4096

# Vector self-metrics for Prometheus scrape (replaces Promtail's :9080).
[sources.vector_metrics]
type = "internal_metrics"

# -----------------------------------------------------------------------------
# Transforms
# -----------------------------------------------------------------------------

# Decode RTX's <PRI>tag msg payload into structured fields.
[transforms.parse]
type = "remap"
inputs = ["rtx_udp"]
source = '''
  # Extract priority and remainder from RTX format: `<PRI>tag and message`.
  # Drop packets that don't start with <NN> (defensive — random UDP noise).
  m = parse_regex(.message, r'^<(?P<pri>\d+)>(?P<rest>.*)') ?? null
  if is_null(m) {
    abort
  }
  pri = to_int!(m.pri)
  sev_num = mod(pri, 8)
  fac_num = floor(pri / 8)
  .severity_num = sev_num
  .facility_num = fac_num

  # RFC 5424 Table 2 severity names; index by severity_num (0..7).
  # VRL requires integer literal for `[...]` indexing; use the `get`
  # function which accepts a runtime path.
  sev_names = ["emerg", "alert", "crit", "err", "warning", "notice", "info", "debug"]
  .severity = get(sev_names, [sev_num]) ?? "unknown"

  # RFC 5424 Table 1 facility names — only local0..local7 of practical
  # interest for RTX (configured via `syslog facility local0`). Unknown
  # facilities fall through to the numeric value as a string.
  fac_names = {
    "0": "kern", "1": "user", "2": "mail", "3": "daemon",
    "4": "auth", "5": "syslog", "6": "lpr", "7": "news",
    "16": "local0", "17": "local1", "18": "local2", "19": "local3",
    "20": "local4", "21": "local5", "22": "local6", "23": "local7",
  }
  .facility = get(fac_names, [to_string(fac_num)]) ?? to_string(fac_num)

  # Replace .message with the post-PRI remainder so downstream regex
  # stages match against the RTX payload only.
  .message = m.rest

  # Map sender IP → device label + device_type. RTX routers: hnd .253 /
  # itm .254. WLX WiFi APs: wlx402 .5 / wlx323 .6. Drop everything else
  # defensively (random LAN devices mis-pointing syslog at this listener
  # should not produce labelled streams). device_type splits the two ES
  # sinks downstream (logs-rtx vs logs-wlx) and is del()'d before each sink.
  #
  # wlx323 was declared .41 until 2026-08-17. That address never belonged to
  # the AP — a tcpdump on the PVE bridge showed the AP sending from .27, and
  # .41 was a Wi-Fi client that appears as a STA in the AP's own
  # ROAMING_ASSIST log. Every wlx323 packet therefore hit the abort below
  # (~200k events/day). Both the AP and this map now use .6, outside the RTX
  # DHCP pool .20-.99, matching how wlx402 sits at .5.
  src_ip = .host
  if src_ip == "192.168.1.253" {
    .router = "hnd"
    .device_type = "router"
  } else if src_ip == "192.168.1.254" {
    .router = "itm"
    .device_type = "router"
  } else if src_ip == "192.168.1.5" {
    .device = "wlx402"
    .device_type = "ap"
  } else if src_ip == "192.168.1.6" {
    .device = "wlx323"
    .device_type = "ap"
  } else {
    abort
  }

  # Stage 1: RTX filter log structure (RTX1210 Rev.14 / RTX830 Rev.15).
  # Actual format observed in 2026-05 packet captures:
  #   "LAN2 Passed at IN(80) filter: TCP 35.79.199.237:443 > 192.168.1.76:49744"
  #   "LAN2 Passed at IN(80) filter: UDP 1.1.1.1:53 > 192.168.1.253:10707 (DNS response)"
  #   "PP[01] Rejected at OUT(200099) filter: ICMP 192.168.1.x > 192.168.1.y"
  # action_word is "Passed" or "Rejected"; normalize to PASS / REJECT to keep
  # the dashboard variable values stable.
  filter_re = r'^(?P<interface>\S+) (?P<action_word>Passed|Rejected) at (?P<direction>IN|OUT)\((?P<filter_no>\d+)\) filter: (?P<protocol>\w+) (?P<src>[\d.]+)(?::(?P<src_port>\d+))? > (?P<dst>[\d.]+)(?::(?P<dst_port>\d+))?'
  fr = parse_regex(.message, filter_re) ?? null
  if !is_null(fr) {
    .event_kind = "filter"
    .ip_version = 4
    .interface = fr.interface
    .direction = fr.direction
    .filter_no = fr.filter_no
    .action = if fr.action_word == "Passed" { "PASS" } else { "REJECT" }
    .protocol = fr.protocol
    .src = fr.src
    if exists(fr.src_port) { .src_port = fr.src_port }
    .dst = fr.dst
    if exists(fr.dst_port) { .dst_port = fr.dst_port }
  }

  # Shared by the IPv6 stages below. RTX prints well-known ports by SERVICE
  # NAME, but src_port / dst_port are `integer` in the logs-rtx mapping, so a
  # bare name must never reach the sink. Names seen on the wire so far are
  # only `www` and `https`; the rest of this table is the RTX service-name
  # vocabulary used by the filter definitions themselves (see rtx-hnd.tf
  # `dest_port = "www,21,nntp"` and the netbios_ns-netbios_ssn ranges), so a
  # firmware that starts printing them resolves rather than drops.
  rtx_ports = { "ftp": 21, "ssh": 22, "telnet": 23, "smtp": 25, "domain": 53,
                "dhcps": 67, "dhcpc": 68, "tftp": 69, "gopher": 70,
                "finger": 79, "www": 80, "pop3": 110, "ident": 113,
                "nntp": 119, "ntp": 123, "netbios_ns": 137,
                "netbios_dgm": 138, "netbios_ssn": 139, "imap2": 143,
                "snmp": 161, "ldap": 389, "https": 443, "syslog": 514,
                "submission": 587, "imaps": 993, "pop3s": 995 }
  msg_rtx = to_string(.message)

  # Stage 1b: IPv6 filter log structure.
  # The Stage 1 regex is IPv4-only — `[\d.]+` for the address and `:` as the
  # port separator — so every IPv6 filter line fell through unparsed until
  # 2026-08-23. That was 382,526 of HND's 384,377 events/day (99.5%) with no
  # action / src / dst / geoip, i.e. invisible to every Kibana aggregation,
  # which is why a steady stream of rejected IPv6 return traffic went
  # unnoticed for weeks. Four shapes occur on the wire:
  #   "LAN2 Rejected at IN(11) filter: TCP 2606:4700:7::1fd.www > 240f:146:70c8:2:3c15:2f95:8ea7:c175.57680"
  #   "LAN2 Rejected at IN(11) filter: UDP 2404:6800:400b:c015::54.https > 240f:146:70c8:2:1cce:c905:53d1:3b6c.61923"
  #   "TUNNEL[1] Rejected at IN(300) filter: ICMP fe80::8617:15ff:fe15:5a5f > ff02::fb : MLDv1 report"
  #   "TUNNEL[1] Rejected at IN(300) filter: Hop-by-hop ext. fe80::8617:15ff:fe15:5a5f > ff02::fb"
  # IPv6 separates address and port with "." rather than ":", and the ICMP /
  # extension-header forms carry no ports at all. `protocol` keeps the token
  # verbatim (ICMP here is really ICMPv6) so existing dashboard filter values
  # keep working; `ip_version` is the field that disambiguates.
  if is_null(fr) {
    f6 = parse_regex(msg_rtx, r'^(?P<interface>\S+) (?P<action_word>Passed|Rejected) at (?P<direction>IN|OUT)\((?P<filter_no>\d+)\) filter: (?P<rest>.+)$') ?? null
    if !is_null(f6) {
      rest6 = to_string(f6.rest)
      # A v6 address token is hex digits and colons only, so it can never
      # swallow the "." that introduces the port nor the " > " separator.
      b6 = parse_regex(rest6, r'^(?P<protocol>TCP|UDP) (?P<src>[0-9A-Fa-f:]+)\.(?P<src_port>[0-9A-Za-z_\-]+) > (?P<dst>[0-9A-Fa-f:]+)\.(?P<dst_port>[0-9A-Za-z_\-]+)$') ?? null
      if is_null(b6) {
        b6 = parse_regex(rest6, r'^(?P<protocol>ICMP) (?P<src>[0-9A-Fa-f:]+) > (?P<dst>[0-9A-Fa-f:]+) : (?P<icmp_type>.+)$') ?? null
      }
      if is_null(b6) {
        # Extension-header and portless forms: "Hop-by-hop ext.", "Fragment
        # ext.", bare "ICMP". The protocol token is lazy so it stops at the
        # first token that parses as a v6 address.
        b6 = parse_regex(rest6, r'^(?P<protocol>[A-Za-z][\w\- ]*?\.?) (?P<src>[0-9A-Fa-f:]+) > (?P<dst>[0-9A-Fa-f:]+)$') ?? null
      }
      if !is_null(b6) {
        src6 = to_string(b6.src)
        dst6 = to_string(b6.dst)
        # Require a colon before writing `src` / `dst`: they are ES `ip`
        # fields and the hex character class also accepts a bare decimal run,
        # which would be a whole-document rejection rather than a lost field.
        if contains(src6, ":") && contains(dst6, ":") {
          .event_kind = "filter"
          .ip_version = 6
          .interface = f6.interface
          .direction = f6.direction
          .filter_no = f6.filter_no
          .action = if f6.action_word == "Passed" { "PASS" } else { "REJECT" }
          .protocol = b6.protocol
          .src = src6
          .dst = dst6
          if exists(b6.icmp_type) { .icmp_type = b6.icmp_type }
          if exists(b6.src_port) {
            sp_tok = to_string(b6.src_port)
            sp = to_int(sp_tok) ?? get(rtx_ports, [sp_tok]) ?? null
            if !is_null(sp) { .src_port = sp }
          }
          if exists(b6.dst_port) {
            dp_tok = to_string(b6.dst_port)
            dp = to_int(dp_tok) ?? get(rtx_ports, [dp_tok]) ?? null
            if !is_null(dp) { .dst_port = dp }
          }
        }
      }
    }
  }

  # Stage 1c: dynamic-filter session log ("[INSPECT]").
  # RTX emits one line per session for every `ip/ipv6 filter dynamic` entry
  # with syslog on, which is the default for HND's v6 entries — 163,311
  # events/day, 43% of that router's volume. It is the only per-session
  # record of WHICH dynamic entry admitted a flow (`dyn_seq`), which is what
  # any stateful-return-path investigation has to join against the
  # terminal-reject lines. v4 prints "addr:port", v6 prints "addr.port":
  #   "[INSPECT] LAN2[out][11] TCP 240f:146:70c8:2:be24:11ff:fe00:67.34944 > 2606:b740:1:20::102.443 (2026/08/23 14:25:32)"
  #   "[INSPECT] LAN2[in][105] UDP 1.1.1.1:53 > 192.168.1.253:10295 (2026/08/22 17:52:13)"
  # The trailing timestamp is the ROUTER's own clock. HND has no ntpdate
  # schedule and measured 296s fast on 2026-08-23, so it is kept in
  # `session_time` and never used for `@timestamp` (which stays receive-time).
  # `direction` is upper-cased to match the IN/OUT of the filter stages.
  if starts_with(msg_rtx, "[INSPECT] ") {
    ins = parse_regex(msg_rtx, r'^\[INSPECT\] (?P<interface>[A-Za-z0-9]+)\[(?P<direction>in|out)\]\[(?P<dyn_seq>\d+)\] (?P<protocol>\w+) (?P<src>[0-9A-Fa-f:]+)\.(?P<src_port>[0-9A-Za-z_\-]+) > (?P<dst>[0-9A-Fa-f:]+)\.(?P<dst_port>[0-9A-Za-z_\-]+) \((?P<session_time>[\d/]+ [\d:]+)\)$') ?? null
    ins_v = 6
    if is_null(ins) {
      ins = parse_regex(msg_rtx, r'^\[INSPECT\] (?P<interface>[A-Za-z0-9]+)\[(?P<direction>in|out)\]\[(?P<dyn_seq>\d+)\] (?P<protocol>\w+) (?P<src>[\d.]+):(?P<src_port>\d+) > (?P<dst>[\d.]+):(?P<dst_port>\d+) \((?P<session_time>[\d/]+ [\d:]+)\)$') ?? null
      ins_v = 4
    }
    if !is_null(ins) {
      .event_kind = "inspect"
      .ip_version = ins_v
      .interface = ins.interface
      .direction = upcase(to_string(ins.direction))
      .dyn_seq = ins.dyn_seq
      .protocol = ins.protocol
      .src = ins.src
      .dst = ins.dst
      isp_tok = to_string(ins.src_port)
      isp = to_int(isp_tok) ?? get(rtx_ports, [isp_tok]) ?? null
      if !is_null(isp) { .src_port = isp }
      idp_tok = to_string(ins.dst_port)
      idp = to_int(idp_tok) ?? get(rtx_ports, [idp_tok]) ?? null
      if !is_null(idp) { .dst_port = idp }
      # RTX renders local time with no offset; the routers are fixed to
      # +09:00 (`rtx_system.timezone` in home-monitor/rtx-hnd.tf), so append
      # it rather than letting parse_timestamp default the value to UTC.
      st = parse_timestamp(to_string(ins.session_time) + "+0900", "%Y/%m/%d %H:%M:%S%z") ?? null
      if !is_null(st) { .session_time = st }
    }
  }

  # Stage 1d: L2MS switch-control log ("[SWCTL]").
  # 5,064 events/day on HND, and `detect up` has never appeared opposite the
  # 11,795 `detect down` in a 7-day window — the same switch MAC shows up
  # under two topology paths (`tn1` and `tn1-lan1:1`) because `switch control
  # use` is on for both lan1 and bridge1, and bridge1 carries the L2TPv3
  # tunnel to the Ibaraki site. Splitting path / MAC / event out is what makes
  # that duplication countable instead of a wall of identical strings.
  #   "[SWCTL] tn1-lan1:1(ac:44:f2:09:f7:3d): detect down"
  #   "[SWCTL] lan1:4(ac:44:f2:39:07:13): PORT2 link down"
  if starts_with(msg_rtx, "[SWCTL] ") {
    sw = parse_regex(msg_rtx, r'^\[SWCTL\] (?P<sw_path>[^()]+)\((?P<sw_mac>[0-9A-Fa-f:]{17})\): (?P<sw_event>.+)$') ?? null
    if !is_null(sw) {
      .event_kind = "swctl"
      .sw_path = sw.sw_path
      .sw_mac = downcase(to_string(sw.sw_mac))
      .sw_event = sw.sw_event
    }
  }

  # Stage 2: IKE / IPsec session events.
  #   "[IKE] initiate IPsec phase to 119.231.58.172"
  #   "[IKE2] SA:5/IKE established"
  #   "[IKE] respond ISAKMP phase to 1.2.3.4 (local address 5.6.7.8)"
  ike_re = r'\[(?P<phase>IKE2?)\] (?P<ike_event>.*?)(?: to (?P<peer>[\d.]+))?(?: \(local address (?P<local>[\d.]+)\))?$'
  ir = parse_regex(.message, ike_re) ?? null
  if !is_null(ir) {
    .phase = ir.phase
    .ike_event = ir.ike_event
    if exists(ir.peer) { .peer = ir.peer }
    if exists(ir.local) { .local = ir.local }
  }

  # Stage 3: DHCPD lease events.
  #   "[DHCPD] Extends 192.168.1.28: 24:59:e5:54:5e:5a"
  dhcp_re = r'\[DHCPD\] (?P<dhcp_event>Extends|Assigns|Releases) (?P<lease_ip>[\d.]+): (?P<mac>[0-9a-f:]+)'
  dr = parse_regex(.message, dhcp_re) ?? null
  if !is_null(dr) {
    .dhcp_event = dr.dhcp_event
    .lease_ip = dr.lease_ip
    .mac = dr.mac
  }

  # Stage 4: `same message repeated N times` collapse marker.
  rep_re = r'same message repeated (?P<repeat_count>\d+) times'
  rr = parse_regex(.message, rep_re) ?? null
  if !is_null(rr) {
    .repeat_count = rr.repeat_count
  }

  # Stage 5: GeoIP enrichment on the extracted filter-log src field.
  # Uses the `dbip_city` enrichment_table declared at the top of this
  # config (dbip-city-lite, CC-BY 4.0, monthly refresh by mitamae).
  # Vector's mmdb enrichment_table returns the raw MaxMind City-DB
  # nested structure (country.iso_code, city.names.en, location.latitude
  # etc.) — flatten the relevant fields to top-level so the downstream
  # Elasticsearch mapping resolves them as scalar fields (geo_point for
  # location, keyword for codes/names).
  # Wrapped in fallible-block style: `parse_regex` and `to_string` for
  # private/invalid IPs return errors that propagate via the `?? null`
  # pattern, leaving the geoip_* fields unset for those events.
  if exists(.src) {
    geoip_data, geoip_err = get_enrichment_table_record("dbip_city", { "ip": to_string!(.src) })
    if geoip_err == null && is_object(geoip_data) {
      .geoip_country_code = get(geoip_data, ["country", "iso_code"]) ?? null
      .geoip_country_name = get(geoip_data, ["country", "names", "en"]) ?? null
      .geoip_continent_code = get(geoip_data, ["continent", "code"]) ?? null
      .geoip_city_name = get(geoip_data, ["city", "names", "en"]) ?? null
      .geoip_latitude = get(geoip_data, ["location", "latitude"]) ?? null
      .geoip_longitude = get(geoip_data, ["location", "longitude"]) ?? null
      # subdivisions is an array; take the first entry (most-specific region).
      subdivs = array(get(geoip_data, ["subdivisions"]) ?? []) ?? []
      if length(subdivs) > 0 {
        first_sub = object(subdivs[0]) ?? {}
        .geoip_subdivision_code = get(first_sub, ["iso_code"]) ?? null
        .geoip_subdivision_name = get(first_sub, ["names", "en"]) ?? null
      }
    }
  }

  # ---------------------------------------------------------------------
  # WLX WiFi AP (WLX402 .5 / WLX323 .6) parsing. Kernel node-management churn
  # is dropped per the 2026-06-06 capture/cost decision. ROAMING_ASSIST was
  # dropped alongside it but is kept again since 2026-08-17: it carries
  # WLX323's roaming decisions (RSSI vs threshold), which is what any
  # AP-to-AP churn analysis rests on, and dropping it while keeping the
  # equivalent ROAMING_11KV Compare lines was inconsistent. Costs ~1 event/s.
  # Both APs are Qualcomm-based;
  # .5 emits `<PRI>[MODULE] msg` (no space after PRI), .6 emits
  # `<PRI> [MODULE] <CODE> msg` (leading space) — strip_whitespace + the
  # optional <CODE> capture handle both. The RTX stages above never match
  # AP messages (different format), so they leave AP events untouched.
  if .device_type == "ap" {
    # `.message` is typed `string | null` (set from m.rest above), so calling
    # strip_whitespace/match/parse_regex on it directly is a fallible
    # assignment (VRL E103) and the running vector rejects the config — note
    # `vector validate --no-environment` does NOT build the topology and so
    # silently passes it. Coerce to a definite string ONCE so every downstream
    # use is infallible.
    msg = strip_whitespace(to_string(.message))
    .message = msg

    # Drop high-volume / low-value lines (kernel node mgmt, driver debug).
    if match(msg, r'(node_cleanup|_ieee80211_free_node|ieee80211_sta_leave|_ieee80211_node_leave|station free|wmi_unified_|net_ratelimit|own address as source|\[0:I:ANY\])') {
      abort
    }

    # Module = first [..] bracket; optional numeric <CODE> after it.
    mm = parse_regex(msg, r'^\[(?P<module>[^\]]+)\]\s*(?P<after>.*)') ?? null
    if !is_null(mm) {
      .module = mm.module
      cm = parse_regex(mm.after, r'^<(?P<code>\d+)>') ?? null
      if !is_null(cm) {
        .code = cm.code
      }
    }

    # STA MAC. Three spellings, tried in order:
    #   app layer   "STA (xx:..)" / "(xx:..)"
    #   disconnect  "(xx:..:auth=4/cipher=4)"
    #   kernel      bare, prefixed by the context that names it — see below
    # The kernel form matters because the single most frequent disconnect
    # signal on WLX402, "sending DEAUTH to <mac>, unknown src reason 6", is
    # written that way and was landing with no sta at all, so the busiest
    # client never appeared in a per-STA breakdown.
    #
    # The prefix alternation is what keeps this safe: a bare MAC also appears
    # in "do ignbaselmac [xx:..] name=ath5", which is the radio's OWN address,
    # not a client — matching that would attribute every client's traffic to
    # the AP. Requiring `vap-N: [` (rather than any `[`) excludes it.
    # Measured over a 10,560-line sample: coverage 62% -> 85%, zero
    # ignbaselmac matches.
    #
    # WLX MACs are already lowercase on the wire, so assign directly (downcase
    # on a string|null capture would be fallible).
    sta_m = parse_regex(msg, r'\((?P<sta>[0-9A-Fa-f:]{17})\)') ?? null
    if !is_null(sta_m) {
      .sta = sta_m.sta
    } else {
      sta_s = parse_regex(msg, r'\((?P<sta>[0-9A-Fa-f:]{17}):auth=') ?? null
      if !is_null(sta_s) {
        .sta = sta_s.sta
      } else {
        sta_k = parse_regex(msg, r'(?:sending DEAUTH to |macaddr |XID leave node |vap-\d+: \[)(?P<sta>[0-9A-Fa-f:]{17})') ?? null
        if !is_null(sta_k) {
          .sta = sta_k.sta
        }
      }
    }

    # AP BSSID: "| AP <xx:..>".
    ap_m = parse_regex(msg, r'\| AP <(?P<ap>[0-9A-Fa-f:]{17})>') ?? null
    if !is_null(ap_m) {
      .ap = ap_m.ap
    }

    # Disassoc / assoc-failure reason code. Two spellings on the wire: the
    # app-layer "Reason Code: 7" and the kernel's "assoc failed with
    # reason=30". Only the first was matched until 2026-08-17, which left the
    # kernel form — the more common one, and the one carrying the temporary
    # assoc rejections — unextracted.
    rc = parse_regex(msg, r'(?:Reason Code:\s*|reason[=\s])(?P<reason_code>\d+)') ?? null
    if !is_null(rc) {
      .reason_code = to_int(rc.reason_code) ?? null
    }

    # Auth/assoc response status code: "<0102> Sent AUTH RES to STA (..).
    # Status Code: 126". 0 means accepted; anything else is the AP's stated
    # reason for turning the client away.
    sc = parse_regex(msg, r'Status Code:\s*(?P<status_code>\d+)') ?? null
    if !is_null(sc) {
      .status_code = to_int(sc.status_code) ?? null
    }

    # DHCP ([MON]): CLT:{mac} + YIP:{ip}.
    cmc = parse_regex(msg, r'CLT:\s*\{(?P<client_mac>[0-9A-Fa-f:]{17})\}') ?? null
    if !is_null(cmc) {
      .client_mac = cmc.client_mac
    }
    yip = parse_regex(msg, r'YIP:\s*\{(?P<client_ip>[\d.]+)\}') ?? null
    if !is_null(yip) {
      .client_ip = yip.client_ip
    }
  }
'''

# Elasticsearch-specific reshaping. Loki accepts everything as strings via
# its label/structured-metadata templating. Elasticsearch's data stream
# `logs-rtx-default` mapping (defined in lxc-elasticsearch cookbook, ADR
# 0005 Phase 3) is `dynamic: "strict"` and types specific fields:
#   *_port    -> integer (so range queries / sort-by-port work)
#   geoip_location -> geo_point (so Kibana Maps / geo aggregations work)
# This transform branches off `parse` so the Loki sink keeps its
# string-shaped fields untouched (no risk of breaking the existing
# rtx-logs.json Grafana dashboard during the dual-write window).
[transforms.parsed_rtx_for_es]
type = "remap"
inputs = ["parse"]
source = '''
  # Only RTX router events belong in logs-rtx; WLX AP events are routed to
  # the wlx sink instead. (2026-06-06)
  if .device_type != "router" {
    abort
  }
  # device_type is a routing helper, not part of the logs-rtx strict
  # mapping — drop it before the sink or the doc is rejected.
  del(.device_type)

  # Convert port fields to integer. Each is best-effort: when the field
  # is absent the to_int! call would error, so we gate on existence.
  # Use ?? null to keep events flowing on conversion failure rather than
  # aborting the whole event.
  if exists(.src_port) {
    .src_port = to_int(.src_port) ?? null
  }
  if exists(.dst_port) {
    .dst_port = to_int(.dst_port) ?? null
  }
  if exists(.peer_port) {
    .peer_port = to_int(.peer_port) ?? null
  }

  # Build geo_point object expected by ES `geo_point` mapping. The Loki
  # sink's templating consumes geoip_latitude / geoip_longitude as
  # separate string labels; ES wants {lat, lon} as a single object on
  # `geoip_location`. Drop the location object entirely if either
  # coordinate is absent (ES strict mapping rejects partial geo_point).
  if exists(.geoip_latitude) && exists(.geoip_longitude) {
    lat, lat_err = to_float(.geoip_latitude)
    lon, lon_err = to_float(.geoip_longitude)
    if lat_err == null && lon_err == null {
      .geoip_location = { "lat": lat, "lon": lon }
    }
  }
'''

# Shape WLX AP events for the logs-wlx-default strict mapping. Branches off
# `parse` (same as parsed_rtx_for_es) so the two ES sinks are independent.
# (2026-06-06)
[transforms.parsed_wlx_for_es]
type = "remap"
inputs = ["parse"]
source = '''
  # Only WLX AP events belong in logs-wlx; RTX router events go elsewhere.
  if .device_type != "ap" {
    abort
  }
  # Routing helper — not in the logs-wlx strict mapping.
  del(.device_type)
'''

# -----------------------------------------------------------------------------
# Sinks
# -----------------------------------------------------------------------------

# Phase 6 (ADR 0005): Loki sink removed. Elasticsearch is the sole sink for
# parsed RTX events; the `[sinks.elasticsearch]` block landed in Phase 4
# along with the `[transforms.parsed_rtx_for_es]` transform that shapes
# events for the `logs-rtx-default` data stream's strict mapping.

# Elasticsearch dual-write sink (ADR 0005 Phase 4). Active alongside
# [sinks.loki] during the 2-week observation window; Phase 6 removes the
# Loki sink after Kibana SLOs are confirmed. Both sinks consume from
# different transforms (`parse` → Loki string-shaped, `parsed_rtx_for_es`
# → ES typed) so failures in one path don't backpressure the other.
#
# Auth: basic-auth via auth.user / auth.password (NOT embedded in the
# endpoint URL — see Adversarial #6). ELASTIC_VECTOR_PASSWORD is read
# from the .env file (mode 0600, SSM-fetched at deploy time). The
# vector_writer ES role grants `monitor` cluster privilege + `write` on
# `logs-rtx-*` indices only.
#
# TLS: ADR-0005 Phase 3b ships ES with transport-only TLS
# (xpack.security.http.ssl.enabled = false). HTTP plain on :9200
# until Phase 7-tls flips http.ssl on with the same CA. Vector therefore
# uses http:// and no `tls = { ... }` block until then. When Phase 7-tls
# ships, flip endpoints back to https:// and uncomment the tls block;
# the bind-mount + cookbook fetch of /etc/vector/elastic-ca.crt is
# already in place to make that change a 3-line revert.
#
# Mode = "data_stream" routes to logs-rtx-default per ECS data-stream
# naming (type-dataset-namespace). The ILM policy logs-rtx-7d
# (defined in lxc-elasticsearch cookbook) handles rollover + 7-day
# delete; Vector does NOT manage the index template or ILM policy.
#
# Buffer: 1 GB disk cap, drop_newest on overflow. Slightly larger than
# Loki because the ES path has higher per-event overhead (JSON-typed
# payload) so transient ingest pauses can accumulate more events before
# the buffer drains.
[sinks.elasticsearch]
type = "elasticsearch"
inputs = ["parsed_rtx_for_es"]
endpoints = [
  "https://es-0.home.local:9200",
  "https://es-1.home.local:9200",
  "https://es-2.home.local:9200",
]
mode = "data_stream"
data_stream = { type = "logs", dataset = "rtx", namespace = "default" }
auth = { strategy = "basic", user = "vector_writer", password = "${ELASTIC_VECTOR_PASSWORD}" }
# Phase 7-tls: HTTPS to ES cluster. CA cert bind-mounted into the
# vector container at /etc/vector/elastic-ca.crt (cookbook fetches
# from SSM /monitoring/elastic/ca/cert).
tls = { ca_file = "/etc/vector/elastic-ca.crt", verify_certificate = true, verify_hostname = true }
buffer = { type = "disk", max_size = 1073741824, when_full = "drop_newest" }

# WLX WiFi AP syslog sink → logs-wlx-default data stream. Same ES cluster,
# same vector_writer credential (its role now grants write on logs-wlx-* too),
# same CA/TLS + disk buffer as the RTX sink. Separate data stream keeps the AP
# assoc/auth/roaming schema isolated from the RTX router schema. (2026-06-06)
[sinks.elasticsearch_wlx]
type = "elasticsearch"
inputs = ["parsed_wlx_for_es"]
endpoints = [
  "https://es-0.home.local:9200",
  "https://es-1.home.local:9200",
  "https://es-2.home.local:9200",
]
mode = "data_stream"
data_stream = { type = "logs", dataset = "wlx", namespace = "default" }
auth = { strategy = "basic", user = "vector_writer", password = "${ELASTIC_VECTOR_PASSWORD}" }
tls = { ca_file = "/etc/vector/elastic-ca.crt", verify_certificate = true, verify_hostname = true }
buffer = { type = "disk", max_size = 1073741824, when_full = "drop_newest" }

# Prometheus scrape endpoint for Vector self-metrics. Replaces Promtail's
# :9080. The lxc-monitoring stack's prometheus.yml job for the syslog
# pipeline targets localhost:9598/metrics.
[sinks.prometheus]
type = "prometheus_exporter"
inputs = ["vector_metrics"]
address = "0.0.0.0:9598"

# -----------------------------------------------------------------------------
# Unit tests (`vector test /etc/vector/vector.toml`)
# -----------------------------------------------------------------------------
#
# These exist because `vector validate --no-environment` does NOT build the
# topology and therefore accepts VRL that the running Vector rejects — the
# trap already documented on the AP branch above. `vector test` compiles the
# remap programs and runs them, so it is the only local gate that actually
# proves a parser change works.
#
# Running them needs the two files the sinks reference (the GeoIP mmdb is
# loaded by the enrichment table at topology build time), so from a checkout:
#
#   docker run --rm
#     -v <repo>/cookbooks/lxc-monitoring/files/vector.toml:/etc/vector/vector.toml:ro
#     -v <geoip dir>:/etc/vector/geoip:ro
#     -v <any pem>:/etc/vector/elastic-ca.crt:ro
#     -e ELASTIC_VECTOR_PASSWORD=test
#     timberio/vector:0.45.0-alpine test /etc/vector/vector.toml
#
# Write the paths out in full rather than using a shell variable: Vector
# substitutes environment references across the WHOLE config file, comments
# included, so a dollar-sign PWD in this block fails config load with
# "Missing environment variable in config".
#
# The mmdb is ~130 MB and not in the repo; copy it off the running host at
# /root/deploy/monitoring/geoip/dbip-city-lite.mmdb.

[[tests]]
name = "rtx v4 filter line still parses (regression)"
[[tests.inputs]]
insert_at = "parse"
type = "log"
[tests.inputs.log_fields]
message = "<133>PP[01] Passed at IN(200099) filter: UDP 157.240.209.16:443 > 192.168.1.152:61373"
host = "192.168.1.254"
[[tests.outputs]]
extract_from = "parse"
[[tests.outputs.conditions]]
type = "vrl"
source = '''
assert_eq!(.router, "itm")
assert_eq!(.event_kind, "filter")
assert_eq!(.ip_version, 4)
assert_eq!(.action, "PASS")
assert_eq!(.direction, "IN")
assert_eq!(.filter_no, "200099")
assert_eq!(.protocol, "UDP")
assert_eq!(.src, "157.240.209.16")
assert_eq!(.src_port, "443")
assert_eq!(.dst, "192.168.1.152")
assert_eq!(.dst_port, "61373")
# Value not asserted: dbip-city-lite is re-downloaded monthly and an
# anycast address can change country between releases. What this pins is
# that Stage 5 still enriches off the Stage 1 `src`.
assert!(exists(.geoip_country_code))
'''

[[tests]]
name = "rtx v6 tcp reject resolves a service-name port"
[[tests.inputs]]
insert_at = "parse"
type = "log"
[tests.inputs.log_fields]
message = "<133>LAN2 Rejected at IN(11) filter: TCP 2606:4700:7::1fd.www > 240f:146:70c8:2:3c15:2f95:8ea7:c175.57680"
host = "192.168.1.253"
[[tests.outputs]]
extract_from = "parse"
[[tests.outputs.conditions]]
type = "vrl"
source = '''
assert_eq!(.router, "hnd")
assert_eq!(.event_kind, "filter")
assert_eq!(.ip_version, 6)
assert_eq!(.action, "REJECT")
assert_eq!(.direction, "IN")
assert_eq!(.filter_no, "11")
assert_eq!(.interface, "LAN2")
assert_eq!(.protocol, "TCP")
assert_eq!(.src, "2606:4700:7::1fd")
assert_eq!(.src_port, 80)
assert_eq!(.dst, "240f:146:70c8:2:3c15:2f95:8ea7:c175")
assert_eq!(.dst_port, 57680)
'''

[[tests]]
name = "rtx v6 udp reject keeps a numeric port and resolves https"
[[tests.inputs]]
insert_at = "parse"
type = "log"
[tests.inputs.log_fields]
message = "<133>LAN2 Rejected at IN(11) filter: UDP 2404:6800:400b:c015::54.https > 240f:146:70c8:2:1cce:c905:53d1:3b6c.61923"
host = "192.168.1.253"
[[tests.outputs]]
extract_from = "parse"
[[tests.outputs.conditions]]
type = "vrl"
source = '''
assert_eq!(.ip_version, 6)
assert_eq!(.protocol, "UDP")
assert_eq!(.src_port, 443)
assert_eq!(.dst_port, 61923)
'''

[[tests]]
name = "rtx v6 icmp keeps the type text and sets no ports"
[[tests.inputs]]
insert_at = "parse"
type = "log"
[tests.inputs.log_fields]
message = "<133>TUNNEL[1] Rejected at IN(300) filter: ICMP fe80::8617:15ff:fe15:5a5f > ff02::fb : MLDv1 report"
host = "192.168.1.253"
[[tests.outputs]]
extract_from = "parse"
[[tests.outputs.conditions]]
type = "vrl"
source = '''
assert_eq!(.ip_version, 6)
assert_eq!(.interface, "TUNNEL[1]")
assert_eq!(.protocol, "ICMP")
assert_eq!(.icmp_type, "MLDv1 report")
assert_eq!(.src, "fe80::8617:15ff:fe15:5a5f")
assert_eq!(.dst, "ff02::fb")
assert!(!exists(.src_port))
assert!(!exists(.dst_port))
'''

[[tests]]
name = "rtx v6 extension-header form parses without ports"
[[tests.inputs]]
insert_at = "parse"
type = "log"
[tests.inputs.log_fields]
message = "<133>TUNNEL[1] Rejected at OUT(300) filter: Hop-by-hop ext. fe80::8617:15ff:fe15:5a5f > ff02::fb"
host = "192.168.1.253"
[[tests.outputs]]
extract_from = "parse"
[[tests.outputs.conditions]]
type = "vrl"
source = '''
assert_eq!(.ip_version, 6)
assert_eq!(.direction, "OUT")
assert_eq!(.protocol, "Hop-by-hop ext.")
assert_eq!(.src, "fe80::8617:15ff:fe15:5a5f")
assert_eq!(.dst, "ff02::fb")
assert!(!exists(.src_port))
'''

[[tests]]
name = "rtx v6 INSPECT session line carries dyn_seq and router-clock session_time"
[[tests.inputs]]
insert_at = "parse"
type = "log"
[tests.inputs.log_fields]
message = "<133>[INSPECT] LAN2[out][11] TCP 240f:146:70c8:2:be24:11ff:fe00:67.34944 > 2606:b740:1:20::102.443 (2026/08/23 14:25:32)"
host = "192.168.1.253"
[[tests.outputs]]
extract_from = "parse"
[[tests.outputs.conditions]]
type = "vrl"
source = '''
assert_eq!(.event_kind, "inspect")
assert_eq!(.ip_version, 6)
assert_eq!(.interface, "LAN2")
assert_eq!(.direction, "OUT")
assert_eq!(.dyn_seq, "11")
assert_eq!(.protocol, "TCP")
assert_eq!(.src, "240f:146:70c8:2:be24:11ff:fe00:67")
assert_eq!(.src_port, 34944)
assert_eq!(.dst, "2606:b740:1:20::102")
assert_eq!(.dst_port, 443)
assert_eq!(.session_time, t'2026-08-23T05:25:32Z')
'''

[[tests]]
name = "rtx v4 INSPECT session line uses the colon port form"
[[tests.inputs]]
insert_at = "parse"
type = "log"
[tests.inputs.log_fields]
message = "<133>[INSPECT] LAN2[in][105] UDP 1.1.1.1:53 > 192.168.1.253:10295 (2026/08/22 17:52:13)"
host = "192.168.1.253"
[[tests.outputs]]
extract_from = "parse"
[[tests.outputs.conditions]]
type = "vrl"
source = '''
assert_eq!(.event_kind, "inspect")
assert_eq!(.ip_version, 4)
assert_eq!(.direction, "IN")
assert_eq!(.dyn_seq, "105")
assert_eq!(.src, "1.1.1.1")
assert_eq!(.src_port, 53)
assert_eq!(.dst_port, 10295)
assert_eq!(.session_time, t'2026-08-22T08:52:13Z')
'''

[[tests]]
name = "rtx SWCTL line splits topology path, mac and event"
[[tests.inputs]]
insert_at = "parse"
type = "log"
[tests.inputs.log_fields]
message = "<134>[SWCTL] tn1-lan1:1(ac:44:f2:09:f7:3d): detect down"
host = "192.168.1.253"
[[tests.outputs]]
extract_from = "parse"
[[tests.outputs.conditions]]
type = "vrl"
source = '''
assert_eq!(.event_kind, "swctl")
assert_eq!(.sw_path, "tn1-lan1:1")
assert_eq!(.sw_mac, "ac:44:f2:09:f7:3d")
assert_eq!(.sw_event, "detect down")
assert!(!exists(.action))
'''

[[tests]]
name = "wlx AP line is untouched by the RTX stages"
[[tests.inputs]]
insert_at = "parse"
type = "log"
[tests.inputs.log_fields]
message = "<134> [ROAMING_ASSIST] <0203> STA (aa:bb:cc:dd:ee:ff) RSSI -72 below threshold"
host = "192.168.1.6"
[[tests.outputs]]
extract_from = "parse"
[[tests.outputs.conditions]]
type = "vrl"
source = '''
assert_eq!(.device, "wlx323")
assert_eq!(.device_type, "ap")
assert_eq!(.module, "ROAMING_ASSIST")
assert!(!exists(.event_kind))
assert!(!exists(.ip_version))
assert!(!exists(.action))
'''

[[tests]]
name = "v6 reject reaches the es sink shaped for the strict mapping"
[[tests.inputs]]
insert_at = "parse"
type = "log"
[tests.inputs.log_fields]
message = "<133>LAN2 Rejected at IN(11) filter: TCP 2606:4700:7::1fd.www > 240f:146:70c8:2:3c15:2f95:8ea7:c175.57680"
host = "192.168.1.253"
[[tests.outputs]]
extract_from = "parsed_rtx_for_es"
[[tests.outputs.conditions]]
type = "vrl"
source = '''
# device_type is a routing helper and is absent from logs-rtx-mappings.json;
# leaving it on the event is a whole-document rejection under dynamic:strict.
assert!(!exists(.device_type))
assert_eq!(.src_port, 80)
assert_eq!(.dst_port, 57680)
assert_eq!(.ip_version, 6)
assert_eq!(.event_kind, "filter")
'''

[[tests]]
name = "swctl event reaches the es sink without router-only filter fields"
[[tests.inputs]]
insert_at = "parse"
type = "log"
[tests.inputs.log_fields]
message = "<134>[SWCTL] lan1:4(ac:44:f2:39:07:13): PORT2 link down"
host = "192.168.1.253"
[[tests.outputs]]
extract_from = "parsed_rtx_for_es"
[[tests.outputs.conditions]]
type = "vrl"
source = '''
assert!(!exists(.device_type))
assert_eq!(.sw_path, "lan1:4")
assert_eq!(.sw_mac, "ac:44:f2:39:07:13")
assert_eq!(.sw_event, "PORT2 link down")
assert!(!exists(.src))
'''
